name: Lint Check

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  lint:
    name: Run Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: pakwheels-mvp/package-lock.json

      - name: Install dependencies
        run: |
          cd pakwheels-mvp
          npm ci

      - name: Run lint
        run: npm run lint
